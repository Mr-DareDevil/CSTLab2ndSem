//Finding out the number of each vowel in the input text


#include<stdio.h>

int main(){

	int value=0;
	while((value+1)>0) value++;
	printf("The largest value that int can store: %d \n", value);
	printf("The smallest value that int can store: %d \n", value+1);

	return 0;
}
